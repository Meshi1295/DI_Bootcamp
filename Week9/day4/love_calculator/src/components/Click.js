import React from 'react'

const Click = (props) => {

        return(
            <>
            <button onClick={props.handelClick}>Click Me</button>
            </>
        )
}

export default Click;