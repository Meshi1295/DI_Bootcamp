const initState = {
  counter:0
  }

export const reducer = (state=initState,action={}) = {
  switch (action.type) {
      case value:
          
          break;
  
      default:
          break;
  }

}